{
  "name": "@particle-academy/prism-workspace",
  "version": "0.1.0",
  "description": "A guarded agent workspace for TypeScript.",
  "type": "module",
  "sideEffects": false,
  "engines": {
    "node": ">=20"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./corpus": {
      "types": "./dist/corpus-runner.d.ts",
      "import": "./dist/corpus-runner.js"
    }
  },
  "files": [
    "dist",
    "src/security/escape-corpus.json",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json && node scripts/copy-assets.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^26.3.0",
    "typescript": "^5.9.3",
    "vitest": "^4.1.11"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Particle-Academy/prism-workspace-ts.git"
  }
}
